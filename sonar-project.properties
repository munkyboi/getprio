# These files intentionally carry parallel contracts: the standalone developer
# portal keeps its own build entrypoint, while the schema bootstrap and its
# forward migration both declare the same database objects for fresh and
# existing deployments. Exclude those expected pairs from copy detection while
# retaining normal bug, vulnerability, and maintainability analysis.
sonar.cpd.exclusions=backend/src/config/env.js,backend/src/config/env.ts,developer-portal/src/DeveloperPortalPage.tsx,database/init.sql,database/migrations/20260919_add_independent_developer_queue_core.sql
