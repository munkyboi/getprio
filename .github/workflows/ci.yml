name: CI

on:
  pull_request:
    branches:
      - main

jobs:
  quality-gate:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: getprio_test
          POSTGRES_PASSWORD: getprio_test
          POSTGRES_USER: getprio_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U getprio_test -d getprio_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      ALLOWANCE_TEST_DATABASE_URL: postgresql://getprio_test:getprio_test@127.0.0.1:5432/getprio_test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Frontend tests
        run: npm run test:frontend

      - name: Platform Dashboard tests
        run: npm run test:platform

      - name: Frontend coverage
        run: npm run test:frontend:coverage

      - name: Backend coverage
        run: npm run test:backend:coverage

      - name: Build frontend and platform dashboard
        run: npm run build

      - name: Build backend
        run: npm run build:backend

  pr-summary:
    needs: quality-gate
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest

    permissions:
      pull-requests: write

    steps:
      - name: Comment PR summary
        uses: actions/github-script@v7
        with:
          script: |
            const conclusion = "${{ needs.quality-gate.result }}";
            const summary =
              conclusion === "success"
                ? "CI passed: lint, typecheck, frontend coverage, backend coverage, and builds succeeded."
                : `CI ${conclusion}: one or more quality checks failed.`;

            const marker = "<!-- getprio-ci-summary -->";
            const body = `${marker}\n${summary}`;
            const { owner, repo } = context.repo;
            const issue_number = context.payload.pull_request.number;

            const comments = await github.paginate(github.rest.issues.listComments, {
              owner,
              repo,
              issue_number
            });

            const existing = comments.find((comment) =>
              comment.body && comment.body.includes(marker)
            );

            if (existing) {
              await github.rest.issues.updateComment({
                owner,
                repo,
                comment_id: existing.id,
                body
              });
            } else {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number,
                body
              });
            }
